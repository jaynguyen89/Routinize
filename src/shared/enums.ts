export enum DATABASE_TABLES {
    SETTINGS = 'settings'
}

export enum STORAGE_KEYS {
    SETTINGS = 'SETTINGS'
}

export enum THEMES {
    DAY,
    NIGHT,
    SEA,
    SKY
}

export enum ORIGIN {
    PERSONAL,
    SHARED
}

export enum MEDIA_TYPES {
    PHOTO,
    VIDEO,
    AUDIO
}

export enum FILE_TYPES {
    PDF,
    TXT,
    DOC,
    PPT,
    EXCEL,
    ARCHIVE,
    CSV,
    CODE,
    OTHERS
}
