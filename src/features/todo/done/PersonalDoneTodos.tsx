import React from "react";
import { connect } from "react-redux";

const mapStateToProps = (state : any) => ({

});

const PersonalDoneTodos = () => {
    return (
        <></>
    );
}

export default connect(
    mapStateToProps
)(PersonalDoneTodos);
