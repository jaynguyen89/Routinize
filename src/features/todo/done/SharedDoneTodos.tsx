import React from "react";
import { connect } from "react-redux";

const mapStateToProps = (state : any) => ({

});

const SharedDoneTodos = () => {
    return (
        <></>
    );
}

export default connect(
    mapStateToProps
)(SharedDoneTodos);
