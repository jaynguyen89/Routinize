import React from "react";
import { connect } from "react-redux";

const mapStateToProps = (state : any) => ({

});

const PersonalActiveTodos = () => {
    return (
        <></>
    );
}

export default connect(
    mapStateToProps
)(PersonalActiveTodos);
