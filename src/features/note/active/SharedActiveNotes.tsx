import React from "react";
import { connect } from "react-redux";

const mapStateToProps = (state : any) => ({

});

const SharedActiveNotes = () => {
    return (
        <></>
    );
}

export default connect(
    mapStateToProps
)(SharedActiveNotes);
