import React from "react";
import { View, Text } from "react-native";

const Settings = () => {
    return (
        <View style={{flex:1}}>
            <Text>Settings Activity</Text>
        </View>
    );
}

export default Settings;
